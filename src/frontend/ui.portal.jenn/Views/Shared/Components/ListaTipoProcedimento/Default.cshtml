  @model IEnumerable<ui.portal.jenn.ViewModel.TipoProcedimentoViewModel>
 
<div class="col-xl-12 col-lg-12 col-md-12">
    <div class="links">

        @if (Model != null && Model.Any())
        {
            @foreach (var item in Model)
            {
                <a href="/Produtos/TipoProcedimentoLista?TipoProcedimentoID=@item.TipoProcedimentoID&TipoProduto=@item.Nome" title="">@item.Nome</a>
            }
        }


    </div>
</div>